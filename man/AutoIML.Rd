% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoiml.R
\name{autoiml}
\alias{autoiml}
\title{Run an AutoIML audit (convenience wrapper)}
\usage{
autoiml(
  task,
  learner,
  resampling = mlr3::rsmp("cv", folds = 5),
  purpose = c("exploratory", "global_insight", "decision_support", "deployment"),
  quick_start = TRUE,
  seed = 1L,
  config = list(),
  verbose = FALSE
)
}
\arguments{
\item{task}{(\link[mlr3:Task]{mlr3::Task})\cr
The task to audit.}

\item{learner}{(\link[mlr3:Learner]{mlr3::Learner})\cr
The learner to audit. For classification, \code{predict_type = "prob"} is required.}

\item{resampling}{(\link[mlr3:Resampling]{mlr3::Resampling})\cr
Resampling strategy used for Gate 1. Defaults to 5-fold CV.}

\item{purpose}{(\code{character(1)})\cr
Declared purpose; controls which gates run and how strict the claim scope is.
One of \code{"exploratory"}, \code{"global_insight"}, \code{"decision_support"}, \code{"deployment"}.}

\item{quick_start}{(\code{logical(1)})\cr
If \code{TRUE}, runs a reduced set of gates for fast iteration.}

\item{seed}{(\code{integer(1)})\cr
Random seed used for reproducible subsampling in computational gates.}

\item{config}{(\code{list()})\cr
Optional nested configuration list merged into the runtime context \code{ctx}
(e.g. \code{list(structure = list(sample_n = 200L))}).}

\item{verbose}{(\code{logical(1)})\cr
If \code{TRUE}, prints a compact run summary after completion.}
}
\value{
\link{AutoIMLResult}.
}
\description{
Convenience wrapper that constructs an \link{AutoIML} object and immediately runs
it.

Use this function if you want a single-call interface. If you want to inspect
or modify configuration prior to running (e.g. \code{ctx$structure}), instantiate
\link{AutoIML} directly.
}
\examples{
library(mlr3)
task = tsk("penguins")
learner = lrn("classif.rpart", predict_type = "prob")
resampling = rsmp("cv", folds = 5)

result = autoiml(task, learner, resampling, purpose = "global_insight", seed = 123)
result$report_card()
}
