% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_analysis_bundle.R
\name{export_analysis_bundle}
\alias{export_analysis_bundle}
\title{Export a comprehensive analysis bundle from a completed AutoIML run}
\usage{
export_analysis_bundle(
  auto,
  dir = "analysis_bundle",
  prefix = "run",
  extra_tables = list(),
  extra_figures = list(),
  fig_width = 10,
  fig_height = 6
)
}
\arguments{
\item{auto}{(\link{AutoIML}) A completed AutoIML runner.}

\item{dir}{(\code{character(1)}) Root output directory (created if needed).}

\item{prefix}{(\code{character(1)}) File name prefix for all outputs.}

\item{extra_tables}{(\code{list}) Named list of \code{list(dt = ..., path = ..., ...)};
each element is forwarded to \code{\link[=save_analysis_table]{save_analysis_table()}}.}

\item{extra_figures}{(\code{list}) Named list of \code{list(p = ..., path = ..., ...)};
each element is forwarded to \code{\link[=save_analysis_plot]{save_analysis_plot()}}.}

\item{fig_width, fig_height}{(\code{numeric(1)}) Default figure dimensions in inches.}
}
\value{
Invisibly, a named list of all paths written.
}
\description{
Saves all standard gate artifact tables, auto-generated plots, and the
audit bundle for any completed \link{AutoIML} run to a directory tree:

\if{html}{\out{<div class="sourceCode">}}\preformatted{dir/
  audit_bundle/   <- export_audit_bundle() output (report card, guide, RDS)
  tables/         <- gate artifact CSVs (G1-G7)
  figures/        <- PDF+PNG pairs for each auto$plot() type
  objects/        <- full result.rds
}\if{html}{\out{</div>}}

All saves are conditional — artifacts that did not run or are absent are
skipped silently. Extend with \code{extra_tables} and \code{extra_figures} for
analysis-specific additions.
}
