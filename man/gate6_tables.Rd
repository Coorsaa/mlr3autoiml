% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gate6_viz.R
\name{gate6_tables}
\alias{gate6_tables}
\title{Gate 6 tables}
\usage{
gate6_tables(x, top_n = 15L)
}
\arguments{
\item{x}{(\link{AutoIML}, \link{AutoIMLResult}, \link{GateResult})\cr
Object containing a Gate 6 result. If a \link{GateResult} is passed, it must be
the Gate 6 result (\code{gate_id == "G6"}).}

\item{top_n}{(\code{integer(1)})
Number of rows to return for the top feature lists (importance).}
}
\value{
A \code{list()} containing (when available):
\itemize{
\item \code{metrics}: Gate 6 metrics table (multiplicity_flag, transport_flag, measure_id).
\item \code{performance}: alternative learner performance summary (mean ± CI).
\item \code{rashomon_set}: subset of learners in the Rashomon set.
\item \code{importance_top}: top features by mean permutation importance across Rashomon models.
\item \code{rankcorr}: pairwise Kendall rank correlations of importance vectors.
\item \code{group_performance}: subgroup performance table (if a \code{group} role is set).
}
}
\description{
Extract key Gate 6 (multiplicity and transport) tables from an AutoIML run.

Gate 6 assesses:
\itemize{
\item \strong{Multiplicity (Rashomon effect)}: whether multiple alternative model classes
achieve near-tie performance under the chosen resampling and primary measure.
\item \strong{Transportability} (lightweight): subgroup performance heterogeneity if the
task has a \code{group} role set.
}

The returned object is intended for programmatic reporting (tables); plotting
is performed via \link{AutoIML} \verb{$plot(type = ...)}.
}
