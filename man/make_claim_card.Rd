% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claim_helpers.R
\name{make_claim_card}
\alias{make_claim_card}
\title{Build a validated claim card for \code{auto$ctx$claim}}
\usage{
make_claim_card(
  purpose,
  semantics,
  stakes,
  claims,
  decision_spec = NULL,
  audience = NULL,
  target_population = NULL,
  setting = NULL,
  time_horizon = NULL,
  transport_boundary = NULL,
  intended_use = NULL,
  intended_non_use = NULL,
  prohibited_interpretations = NULL,
  decision_policy_rationale = NULL,
  human_factors_evidence = NULL
)
}
\arguments{
\item{purpose}{(\code{character(1)}) Analysis purpose. One of \code{"exploratory"},
\code{"global_insight"}, \code{"decision_support"}, \code{"deployment"}.}

\item{semantics}{(\code{character(1)}) Interpretation semantics. One of
\code{"within_support"}, \code{"marginal_model_query"}, \code{"causal_recourse"}.}

\item{stakes}{(\code{character(1)}) Stakes level. One of \code{"low"}, \code{"medium"},
\code{"high"}.}

\item{claims}{(\code{list}) Named logical list with elements \code{global}, \code{local},
\code{decision} (e.g. \code{list(global = TRUE, local = TRUE, decision = FALSE)}).}

\item{decision_spec}{(\code{list} | \code{NULL}) Decision context for G3: named list
with at least \code{thresholds} (numeric vector). Required when
\code{claims$decision = TRUE}.}

\item{audience}{(\code{character(1)} | \code{NULL}) Intended audience description.}

\item{target_population}{(\code{character(1)} | \code{NULL}) Target population.}

\item{setting}{(\code{character(1)} | \code{NULL}) Deployment / analysis setting.}

\item{time_horizon}{(\code{character(1)} | \code{NULL}) Decision / prediction
time horizon.}

\item{transport_boundary}{(\code{character(1)} | \code{NULL}) Explicit transport
boundary statement.}

\item{intended_use}{(\code{character(1)} | \code{NULL}) Stated intended use.}

\item{intended_non_use}{(\code{character(1)} | \code{NULL}) Explicit non-uses.}

\item{prohibited_interpretations}{(\code{character(1)} | \code{NULL}) Interpretations
explicitly ruled out (e.g. causal claims).}

\item{decision_policy_rationale}{(\code{character(1)} | \code{NULL}) Rationale for
the decision policy / utility framing.}

\item{human_factors_evidence}{(\code{list} | \code{NULL}) Human-factors evidence
for G7B (user-facing audiences). Named list with fields \code{participants},
\code{task_design}, \code{baseline}, \code{outcomes}, \code{analysis_summary}.}
}
\value{
A named list ready to assign to \code{auto$ctx$claim}.
}
\description{
Constructs a named list suitable for assignment to \code{auto$ctx$claim}. The
four operative fields — \code{purpose}, \code{semantics}, \code{stakes}, \code{claims} — are
required and validated. All narrative documentation fields are optional.
When they are omitted, Gate 0A derives conservative defaults from the
declared purpose, semantics, and decision specification, and users can
override those defaults whenever the analysis warrants narrower wording.
}
\examples{
card = make_claim_card(
  purpose = "decision_support",
  semantics = "within_support",
  stakes = "medium",
  claims = list(global = TRUE, local = FALSE, decision = TRUE),
  decision_spec = list(
    thresholds = seq(0.05, 0.25, by = 0.05),
    utility    = list(tp = 1, fp = 0, fn = 0, tn = 0)
  )
)
}
