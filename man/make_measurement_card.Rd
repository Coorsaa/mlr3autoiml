% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claim_helpers.R
\name{make_measurement_card}
\alias{make_measurement_card}
\title{Build a measurement card for \code{auto$ctx$measurement}}
\usage{
make_measurement_card(
  level,
  missingness_plan = NULL,
  reliability = NULL,
  invariance = NULL,
  scoring_pipeline = NULL
)
}
\arguments{
\item{level}{(\code{character(1)}) Measurement level. One of \code{"item"},
\code{"scale"}, \code{"factor_score"}, \code{"plausible_values"}.}

\item{missingness_plan}{(\code{character(1)} | \code{NULL}) How missing data are
handled in the pipeline.}

\item{reliability}{(\code{list} | \code{character(1)} | \code{NULL}) Reliability evidence
(e.g. \code{list(method = "Cronbach alpha", value = 0.87)}).}

\item{invariance}{(\code{list} | \code{character(1)} | \code{NULL}) Measurement invariance
status across groups / time points.}

\item{scoring_pipeline}{(\code{character(1)} | \code{NULL}) How the outcome and
predictors are scored / encoded.}
}
\value{
A named list ready to assign to \code{auto$ctx$measurement}.
}
\description{
Constructs a named list suitable for assignment to \code{auto$ctx$measurement}.
\code{level} is required and operative (affects G0B checks and narration scope).
All other fields are narrative documentation stored in the G0B artifact.
}
\examples{
card <- make_measurement_card(
  level            = "scale",
  missingness_plan = "Median impute numerics, mode impute categoricals.",
  reliability      = list(method = "Cronbach alpha", note = "alpha = 0.87"),
  invariance       = list(status = "partial", note = "Loadings invariant, intercepts not tested.")
)
}
